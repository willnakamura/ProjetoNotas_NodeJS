/*
const fs = require('fs');
fs.writeFileSync('note.txt', 'Mensagem aqui!');
fs.appendFileSync('note.txt', 'Segunda mensagem');
*/

const getNotes = () => {return 'Your notes...'};
module.exports = getNotes;